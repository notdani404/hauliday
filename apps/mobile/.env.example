# Copy to apps/mobile/.env (gitignored). Expo inlines EXPO_PUBLIC_* at build time.
# The anon key is publishable (safe in the client bundle). NEVER put the
# service_role key here.
EXPO_PUBLIC_SUPABASE_URL=https://gfmonmctebklltywhhzn.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
